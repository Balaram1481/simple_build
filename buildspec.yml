version: 0.2

phases:
  build:
    commands:
      - echo "Running the batch script..."
      - chmod +x script
      - chmod +x script.bat
      - ./script
      - ./script.bat

artifacts:
  files:
    - '**/*'
